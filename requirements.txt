# Core dependencies
pandas
numpy
tqdm

# HuggingFace Transformers + SentenceTransformers
transformers>=4.30.0
sentence-transformers>=2.2.2

# Metrics
bert-score>=0.3.13

# Torch (CPU/GPU depending on environment)
torch>=2.0.0

# Optional: scikit-learn for additional analysis
scikit-learn

# Additional dependencies for drivers and fine-tuning
openai  # required for GPT-4o driver
google-generativeai  # required for Gemini driver
huggingface-hub>=0.15.1  # required for Allam/LLaMA/Mistral/DeepSeek/Jais drivers
datasets>=2.15.0  # used in fine-tuning script
peft>=0.10.0  # used for PEFT adapters in evaluation
